package GenerycTask;

public final class Algorithm {

    /*public static <T>T max(T x,T y){

        return x>y ? x:y;
    }

     */
    //It is not compile because generyc accept only object. Thats why it is now run
    // ">" we can not use this operator for non primitive
}
